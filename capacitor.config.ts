import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chiriboga.digipedia',
  appName: 'Digipedia',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
